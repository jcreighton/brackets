var React = require('react');

var ProfileContainer = React.createClass({
  render: function() {
    return (
      <div>
        PROFILE & MESSAGES
      </div>
    );
  }
});

module.exports = ProfileContainer;