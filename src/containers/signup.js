var React = require('react');

var SignUpContainer = React.createClass({
  render: function() {
    return (
      <div>
        SIGN UP FORM
      </div>
    );
  }
});

module.exports = SignUpContainer;